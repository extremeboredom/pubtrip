<h1 class="page-header">Add member to <%= @group.name %></h1>

<%= form_for [@group, @member] do |f| %>
  <fieldset>
    <%= f.label :user %>
    <%= f.text_field :user %>
  </fieldset>
    <%= f.submit nil, :class => 'btn btn-primary' %>
<% end %>